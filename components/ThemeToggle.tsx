import React, { useEffect, useState } from 'react';

const ThemeToggle = () => {
  // Remove toggle functionality
  return null; // No toggle needed for dark theme only
};

export default ThemeToggle;
